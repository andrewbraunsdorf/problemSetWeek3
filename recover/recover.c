#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    if (argc != 2)
    {
        fprintf(stderr, "Usage: \"./recover <image>\" should have exactly one prompt after recover\n");
        return 1;
    }

}
